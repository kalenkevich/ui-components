export default theme => ({
  root: {
    width: '100%',
    height: '500px',
    display: 'flex',
    flexDirection: 'column',
    border: theme.border,
    borderRadius: theme.borderRadius,
  },
});
